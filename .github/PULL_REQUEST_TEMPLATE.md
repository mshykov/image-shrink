<!-- What changed and why. The why is the part a reviewer cannot reconstruct. -->

## Checked

- [ ] `./scripts/smoke-test.sh` passes
- [ ] If the window changed: a render from `./scripts/design-probe.sh` is attached
- [ ] If the behaviour changed: `CHANGELOG.md` has a line under the unreleased version
- [ ] Colours, spacing, radii and motion come from `Theme.swift`, not from literals
