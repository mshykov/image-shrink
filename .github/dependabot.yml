# The workflows pin their actions by commit SHA, which is the safe way to use them and also
# the way that never updates itself. This is what moves the pins.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns: ["*"]
